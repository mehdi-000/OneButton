.minimap-chrome {
    width: 100%;
    height: 100%;
    display: flex;
    flex-direction: row;
    padding: 3px;
    overflow: hidden;
    background-color: rgba(6, 10, 22, 1);
    border-width: 3px;
    border-color: rgba(120, 220, 255, 0.95);
    border-radius: 50%;
    opacity: 0;
    scale: 0.7 0.7;
    transition-property: opacity, scale;
    transition-duration: 0.3s, 0.35s;
    transition-timing-function: ease-out, ease-out;
}

.minimap-chrome.minimap-visible {
    opacity: 1;
    scale: 1 1;
    border-radius: 50%;
}

.minimap-close-view {
    flex-grow: 1;
    width: 100%;
    height: 100%;
    overflow: hidden;
    border-radius: 50%;
    background-color: rgba(6, 10, 22, 1);
    -unity-background-scale-mode: stretch-to-fill;
    -unity-background-image-tint-color: rgb(210, 235, 255);
}
